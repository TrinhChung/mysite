version: 2.1
orbs:
  python: circleci/python@1.5.0
  docker: circleci/docker@2.2.0
jobs:
  django-test:
    docker:
      - image: cimg/python:3.10.1
        name: python
      - image: cimg/mysql:8.0
        name: mysql
        command: -p 3306:3306 --default-authentication-plugin=mysql_native_password
        environment:
          MYSQL_DATABASE: mysite
          MYSQL_USER: siteuser
          MYSQL_PASSWORD: chungtrinh1904
          MYSQL_ROOT_PASSWORD: chungtrinh1904
    steps:
      - checkout
      - run:
          name: Waiting for MySQL to be ready
          command: |
            for i in `seq 1 30`;
            do
              nc -z 127.0.0.1 3306 && echo Success && exit 0
              echo -n .
              sleep 1
            done
            echo Failed waiting for MySQL && exit 1
      # - run:
      #     name: install dependencies
      #     command: pip install -r requirements.txt
      # - run:
      #     name: run tests
      #     command: python manage.py test --noinput --keepdb

  build-and-push:
    environment:
      DOCKER_IMAGE: trinhchung/cicd
      DOCKER_TAG: 0.0.1
    executor: docker/docker
    steps:
      - setup_remote_docker
      - checkout
      - docker/check:
          docker-username: DOCKER_USER
          docker-password: DOCKER_PASSWORD
      - docker/build:
          image: $DOCKER_IMAGE
          tag: $DOCKER_TAG
      - docker/push:
          digest-path: /tmp/digest.txt
          image: $DOCKER_IMAGE
          tag: $DOCKER_TAG

      - run:
          command: |
            echo "Digest is: $(</tmp/digest.txt)"
workflows:
  build-deploy:
    jobs:
      - django-test
      # - build-and-push:
      #     requires:
      #       - django-test
      #     filters:
      #       branches:
      #         only:
      #           - circleci-project-setup
